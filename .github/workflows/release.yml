name: Release

# Tagged releases: v* → named version release with binary assets
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Lint
        run: scripts/lint.sh

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Test
        run: scripts/test.sh

  build:
    name: Build Release Binary
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build release
        run: cargo build --release

      - name: Strip binary
        run: strip target/release/remendo-tui

      - name: Package archive
        run: |
          RELEASE_NAME="remendo-tui-${{ github.ref_name }}-linux-x86_64"
          mkdir -p "${RELEASE_NAME}"
          cp target/release/remendo-tui "${RELEASE_NAME}/"
          cp README.md config.example.toml "${RELEASE_NAME}/" 2>/dev/null || true
          [ -f LICENSE ] && cp LICENSE "${RELEASE_NAME}/" || true
          tar czf "${RELEASE_NAME}.tar.gz" "${RELEASE_NAME}/"
          echo "RELEASE_NAME=${RELEASE_NAME}" >> $GITHUB_ENV
          echo "ARCHIVE=${RELEASE_NAME}.tar.gz" >> $GITHUB_ENV

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-binary
          path: "*.tar.gz"

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v6

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: release-binary

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "remendo-tui ${{ github.ref_name }}"
          body: |
            ## remendo-tui ${{ github.ref_name }}

            ### Download

            Download the Linux x86_64 binary from the assets below.

            ### Quick start

            ```sh
            tar xzf remendo-tui-${{ github.ref_name }}-linux-x86_64.tar.gz
            cd remendo-tui-${{ github.ref_name }}-linux-x86_64
            ./remendo-tui
            ```
          files: "*.tar.gz"
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
