# thread-indentation baseline test definitions

[metadata]
feature = "thread-indentation"
tier = "baseline"
series = "series7-polish-and-ux"
generated_by = "multi_feature.test_definition_generation"

[[steps]]
name = "root_message_no_indent"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Create thread with a root message (in_reply_to=None) — verify build_depth_map assigns depth 0 and no indent is applied."
acceptance_criteria_ref = "PLAN-01 criterion: root messages at depth 0"

[[steps]]
name = "reply_indented_one_level"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Create thread: parent (depth 0) → child (in_reply_to=parent) — verify child has depth 1 and indent of '  ' (2 spaces)."
acceptance_criteria_ref = "PLAN-01 criterion: reply indented 2 spaces per depth level"

[[steps]]
name = "nested_reply_two_levels"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Create thread: A → B → C (3 levels) — verify depths are 0, 1, 2 and indents are 0, 2, 4 spaces respectively."
acceptance_criteria_ref = "PLAN-01 criterion: depth increments per reply level"

[[steps]]
name = "siblings_same_depth"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Create thread: parent → sibling_A, parent → sibling_B — verify both siblings have depth 1."
acceptance_criteria_ref = "PLAN-01 criterion: sibling replies at same depth"

[[steps]]
name = "depth_capped_at_five"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "edge-case"
description = "Create a thread chain 8 levels deep — verify depth is capped at 5 (indent of 10 spaces max)."
acceptance_criteria_ref = "PLAN-01 criterion: depth capped at 5"

[[steps]]
name = "orphan_message_depth_one"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "edge-case"
description = "Create message with in_reply_to pointing to a non-existent message_id — verify build_depth_map assigns depth 1 (orphan fallback)."
acceptance_criteria_ref = "PLAN-01 criterion: orphan messages get depth 1"

[[steps]]
name = "message_no_message_id_depth_zero"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "edge-case"
description = "Create message with message_id=None — verify it is treated as root (depth 0) and not inserted into depth_map."
acceptance_criteria_ref = "PLAN-01 criterion: no message_id → depth 0"

[[steps]]
name = "two_lines_per_message_invariant"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Render a thread with 3 messages at varying depths — verify exactly 6 Line objects are produced (2 per message), preserving the line+=2 invariant."
acceptance_criteria_ref = "PLAN-01 criterion: line += 2 invariant preserved"

[[steps]]
name = "flat_thread_no_visual_change"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "Create thread where all messages have in_reply_to=None — verify all render at depth 0, visually identical to pre-feature behavior."
acceptance_criteria_ref = "PLAN-01 criterion: flat threads render identically"

[[steps]]
name = "empty_thread_no_output"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "edge-case"
description = "Call detail_thread_lines with empty thread vec — verify it returns early with no Line objects pushed."
acceptance_criteria_ref = "PLAN-01 criterion: empty thread handled"

[[steps]]
name = "comment_navigation_n_N_still_works"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "With indented thread, verify compute_comment_positions still returns correct positions (every 2 lines) — n/N navigation jumps to correct messages."
acceptance_criteria_ref = "PLAN-01 criterion: compute_comment_positions invariant holds"

[[steps]]
name = "indent_rendered_as_span_raw"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "rendering"]
group = "happy-path"
description = "Render a depth-2 message — verify the Line starts with Span::raw containing '    ' (4 spaces) followed by the styled author span."
acceptance_criteria_ref = "PLAN-01 criterion: indentation via leading Span::raw"

[[steps]]
name = "author_and_date_on_first_line"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "For each thread message, verify line 1 contains indent + author (accent) + date (muted) spans."
acceptance_criteria_ref = "PLAN-01 criterion: line 1 is author+date with indent"

[[steps]]
name = "subject_on_second_line"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view"]
group = "happy-path"
description = "For each thread message, verify line 2 contains indent + subject (foreground) span."
acceptance_criteria_ref = "PLAN-01 criterion: line 2 is subject with indent"
