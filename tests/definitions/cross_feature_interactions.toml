# Cross-feature interaction test definitions for series7-polish-and-ux

[metadata]
feature = "cross-feature"
tier = "baseline"
series = "series7-polish-and-ux"
generated_by = "multi_feature.test_definition_generation"

[[steps]]
name = "sort_then_bookmark_filter_compose_order"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "Sort by Date ascending, then toggle bookmark filter on — verify bookmarked patchsets appear in date-sorted order (sort applied first via in-place mutation, filter second via iterator predicate)."
acceptance_criteria_ref = "PLAN-01 column-sorting: sort-first, filter-second compose order"

[[steps]]
name = "bookmark_filter_then_sort_change"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "Toggle bookmark filter on, then cycle sort column — verify filtered rows re-sort by new column while maintaining bookmark predicate."
acceptance_criteria_ref = "PLAN-01 bookmark-filtering: compose with sort"

[[steps]]
name = "sort_reverse_with_bookmark_filter_active"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "With bookmark filter active and sort on Author ascending, press 'S' to reverse — verify bookmarked rows now appear in reverse author order."
acceptance_criteria_ref = "PLAN-01 column-sorting: reverse sort + PLAN-01 bookmark-filtering: compose with sort"

[[steps]]
name = "switch_remote_resets_both_sort_and_filter"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "Set sort to Findings/Descending and bookmark filter on, then switch remote — verify both sort_column resets to Default, sort_direction to Ascending, and show_bookmarks_only to false."
acceptance_criteria_ref = "PLAN-01 column-sorting: switch_remote reset + PLAN-01 bookmark-filtering: switch_remote reset"

[[steps]]
name = "page_load_preserves_sort_and_filter"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "Set sort to Status/Ascending and bookmark filter on, then load new page — verify new page items are sorted by status and only bookmarked items pass the filter."
acceptance_criteria_ref = "PLAN-01 column-sorting: apply_sort on page load + PLAN-01 bookmark-filtering: filter persists"

[[steps]]
name = "bookmark_filter_empty_after_sort"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-list-view"]
group = "cross-feature"
description = "Sort by Author, toggle bookmark filter with no bookmarks — verify empty table rendered, sorted items still exist in patchsets.items but all filtered out."
acceptance_criteria_ref = "PLAN-01 bookmark-filtering: zero matching bookmarks + sort compose"

[[steps]]
name = "diff_highlighting_independent_of_list_view_features"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["diff-syntax-highlighting", "column-sorting", "bookmark-filtering", "tui-detail-view"]
group = "cross-feature"
description = "With sort and bookmark filter active in list view, open a patchset detail and view a message with diff — verify diff syntax highlighting renders correctly (the features are in different views and must not interfere)."
acceptance_criteria_ref = "PLAN-01 diff-syntax-highlighting: independent of list view features"

[[steps]]
name = "detail_view_independent_of_list_view_state"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["column-sorting", "bookmark-filtering", "tui-detail-view"]
group = "cross-feature"
description = "With sort and bookmark filter active, navigate to detail view — verify thread rendering and compute_comment_positions are not affected by list view state."
acceptance_criteria_ref = "Detail view rendering independent of list view sort/filter state"

[[steps]]
name = "diff_highlighting_and_thread_in_same_detail_view"
type = "interaction"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["diff-syntax-highlighting", "tui-detail-view"]
group = "cross-feature"
description = "Open a patchset detail with both a thread and inline reviews containing diff — verify diff syntax highlighting in the review section renders correctly alongside the thread section."
acceptance_criteria_ref = "Diff highlighting in inline reviews coexists with thread rendering in detail view"
