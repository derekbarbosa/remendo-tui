# thread-indentation stress test definitions

[metadata]
feature = "thread-indentation"
tier = "stress"
series = "series7-polish-and-ux"
generated_by = "multi_feature.test_definition_generation"

[[steps]]
name = "thread_50_messages_linear_chain"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "resource-exhaustion"
description = "Create a linear reply chain of 50 messages — verify all depths computed correctly (capped at 5 after depth 5) and no performance degradation."
acceptance_criteria_ref = "PLAN-01 criterion: depth cap at 5, O(n) algorithm"

[[steps]]
name = "thread_all_orphans"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "adversarial-input"
description = "Create thread where every message's in_reply_to references a non-existent ID — verify all get depth 1 (orphan) and no panic."
acceptance_criteria_ref = "PLAN-01 criterion: orphan fallback depth 1"

[[steps]]
name = "thread_child_before_parent"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "adversarial-input"
description = "Create thread where child appears before parent in the slice — verify child gets depth 1 (graceful degradation, parent not yet in map)."
acceptance_criteria_ref = "PLAN-01 criterion: single-pass algorithm, graceful degradation"

[[steps]]
name = "thread_self_referencing_message"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "adversarial-input"
description = "Create message where in_reply_to equals its own message_id — verify depth computed without infinite loop (self is not in map when processed, so orphan depth 1)."
acceptance_criteria_ref = "PLAN-01 criterion: build_depth_map processes in order"

[[steps]]
name = "thread_duplicate_message_ids"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "adversarial-input"
description = "Create thread with two messages sharing the same message_id — verify second insert overwrites first in HashMap and no panic."
acceptance_criteria_ref = "PLAN-01 criterion: HashMap keyed by message_id"

[[steps]]
name = "thread_no_message_ids_at_all"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "edge-case"
description = "Create thread where all messages have message_id=None — verify all get depth 0 and empty depth_map."
acceptance_criteria_ref = "PLAN-01 criterion: no message_id → depth 0, not inserted"

[[steps]]
name = "thread_wide_tree_10_siblings"
type = "positive"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "resource-exhaustion"
description = "Create root with 10 direct replies — verify all siblings at depth 1 and rendering produces 22 Lines (1 root × 2 + 10 siblings × 2)."
acceptance_criteria_ref = "PLAN-01 criterion: siblings same depth, line count invariant"

[[steps]]
name = "thread_unicode_message_ids"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "adversarial-input"
description = "Create thread with Unicode message_id and in_reply_to values — verify HashMap lookup works correctly."
acceptance_criteria_ref = "PLAN-01 criterion: message_id is String, HashMap handles any content"

[[steps]]
name = "depth_exactly_at_cap"
type = "edge"
verification_layer = "integration-artifact-runtime"
proof_depth = "functional"
executor = "host-native"
tags = ["thread-indentation", "tui-detail-view", "stress"]
group = "edge-case"
description = "Create thread exactly 5 levels deep — verify depth is 5 and indent is '          ' (10 spaces). Add one more level — verify still capped at 5."
acceptance_criteria_ref = "PLAN-01 criterion: depth.min(5)"
